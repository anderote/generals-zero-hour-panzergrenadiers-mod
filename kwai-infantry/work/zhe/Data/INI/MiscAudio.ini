
MiscAudio
  ; played when the radar indicates a unit is under attack
  RadarNotifyUnitUnderAttackSound = RadarNotifyUnitUnderAttack
  ; played when the radar indicates a harvester is under attack
  RadarNotifyHarvesterUnderAttackSound = RadarNotifyHarvesterUnderAttack
  ; played when the radar indicates a structure is under attack
  RadarNotifyStructureUnderAttackSound = RadarNotifyStructureUnderAttack
  ; played when the radar indicates something is under attack
  RadarNotifyUnderAttackSound = RadarNotifyUnderAttack
  ; played when the radar indicates something is infiltrated
  RadarNotifyInfiltrationSound = NoSound
  ; played when radar goes online
  RadarNotifyOnlineSound = RadarOn
  ; played when radar goes offline
  RadarNotifyOfflineSound = RadarOff
  ; played when a stealth fighter is doing lockon
  LockonTickSound = LockonTick
  ; played when a unit is defecting, etc.
  DefectorTimerTickSound = DefectorTimerTick
  ; played when the units cheer
  AllCheerSound = UI_AllCheerSound
  ; played for battlecry
  BattleCrySound = BattleCrySound
  ; played when user presses a button in UI
  GUIClickSound = GUIClick
  ; played when user can't perform an action
  NoCanDoSound = NoCanDoSound
  ; I just discovered a stealthed enemy unit
  StealthDiscoveredSound = StealthDiscoveredSound
  ; My stealth unit was just discovered by the enemy
  StealthNeutralizedSound = StealthNeutralizedSound
  ; Money has been deposited in my account
  MoneyDepositSound = MoneyDeposited
  ; Money has been withdrawn from my account
  MoneyWithdrawSound = MoneyWithdraw
  ; Building has lost power, been hit with an EMP, or disable hacked.
  BuildingDisabled = BuildingDisabled
  ; Building has recovered from being disabled.
  BuildingReenabled = BuildingEnabled
  ; Vehicle has been disabled via EMP or hacker attack.
  VehicleDisabled = BuildingDisabled
  ; Vehicle has recovered from being disabled.
  VehicleReenabled = BuildingEnabled
  ; Pilot has been sniped by Jarmen Kell.
  SplatterVehiclePilotsBrain = VehicleSnipeVictim
  ; Terrorist in car has been ordered to move.
  TerroristInCarMoveVoice = CarBombVoiceMove
  ; Terrorist in car has been ordered to attack.
  TerroristInCarAttackVoice = CarBombVoiceAttack
  ; Terrorist in car has been selected.
  TerroristInCarSelectVoice = CarBombVoiceSelect
  ; When unit picks up a heal crate.
  CrateHeal = CrateHeal
  ; When unit picks up a shroud crate.
  CrateShroud = CrateShroud
  ; When unit picks up a salvage crate.
  CrateSalvage = SalvageUpgrade
  ; When unit picks up a free unit crate.
  CrateFreeUnit = NoSound ;CrateFreeUnit
  ; When unit picks up a money crate.
  CrateMoney = CrateMoney
  ; When unit gets promoted
  UnitPromoted = UnitPromoted
  ; When battle drone repairs unit.
  RepairSparks = RepairSparks
  ; When Saboteur hits a building he shuts down
  SabotageShutDownBuilding = SabotageBuildingPower
  ; When Saboteur hits a building he resets the timer on
  SabotageResetTimeBuilding = SabotageBuilding
  ; Plays whenever aircraft land.
  AircraftWheelScreech = JetSkid
End
